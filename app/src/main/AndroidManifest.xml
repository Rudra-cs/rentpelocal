<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.rudra.rentpe">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.RentPe">
        <activity
            android:name=".views.activities.Dashboard"
            android:exported="false" />
        <activity
            android:name=".views.activities.LoginPage"
            android:exported="false" />
        <activity
            android:name=".views.activities.optionPage"
            android:exported="false" />
        <activity
            android:name=".views.activities.SplashScreen"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".views.activities.MainActivity"
            android:exported="false" />
        <activity android:name=".views.activities.onboardingScreeing" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>